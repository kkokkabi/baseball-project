const { Pool } = require('pg');

// Render 환경에서는 환경변수 DATABASE_URL을 자동으로 읽어옵니다.
const pool = new Pool({
    connectionString: process.env.DATABASE_URL,
    ssl: { rejectUnauthorized: false }, // Neon/Render 연결 필수 설정
});

module.exports = pool;
